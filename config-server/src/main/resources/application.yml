server:
  port: 10001
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/yan981471305/spring-cloud/tree/master/config
  #        ignore-local-ssh-settings: true
  #        strictHostKeyChecking: false
  #        force-pull: true
          search-paths: '{application}'
    #      clone-on-start: true
          username: yan981471305
          password: yan199436

eureka:
  client:
    service-url:
      defaultZone: http://localhost:10000/eureka/

